# Rust build artifacts
/target/
/bin/
Cargo.lock
**/*.rs.bk

# Assembly output
/build/

# User overrides
config.yaml
**/user/

# Secrets
.env

# Provider directories (generated by install)
.claude/
.gemini/
.codex/
.opencode/

# Node
package-lock.json
node_modules/

# Plans
docs/plans/

# Parallel feature work
.worktrees/

# Entire session data (runtime, not source). The hooks Entire installs into
# .githooks/ are tracked source; only its runtime data here is ignored.
.entire/

# SpecStory session capture (separate tool, runtime)
.specstory/

# macOS
.DS_Store
