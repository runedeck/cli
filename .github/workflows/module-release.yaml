name: Module Release

on:
    workflow_call:
        inputs:
            rune-version:
                description: "rune-cli version to use"
                type: string
                default: "latest"

permissions:
    contents: write
    id-token: write
    attestations: write

jobs:
    release:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6

            - uses: runedeck/rune/.github/actions/setup-rune@main
              with:
                  version: ${{ inputs.rune-version }}

            - name: Validate
              run: make validate

            - name: Release
              run: rune release

            - name: Attest build provenance
              uses: actions/attest-build-provenance@v4
              with:
                  subject-path: build/*.tar.gz

            - name: Create GitHub release
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  VERSION: ${{ github.ref_name }}
              run: gh release create "$VERSION" build/*.tar.gz --title "$VERSION" --generate-notes
