Scaffold a project from skeleton archetypes

Usage: rune init [OPTIONS] [SLUG_OR_DIR]

Arguments:
  [SLUG_OR_DIR]  Project slug or directory (created if missing)

Options:
      --module <DIR>           Scaffold a rune module for deck authoring
      --with <NAME[,NAME...]>  Ordered template names to compose after base. Repeatable or comma-separated
      --lang <LANG>            Compatibility alias selecting a language template [possible values: rust, python, shell]
      --purpose <PURPOSE>      Compatibility alias selecting a purpose template [possible values: tool, module, spine]
      --skeleton <DIR>         Skeleton repository root
      --brief <BRIEF>          Short project description used for ${BRIEF} [default: ""]
      --bind                   Bind the scaffolded project as the active target
      --workshop               Workshop scaffold: private/public/assets layout, jj colocation, no automatic commit. Defaults on under the targets root
      --spine                  VCS spine (jj colocation) for a plain project
      --dry-run                Print the scaffold plan without writing to the destination
      --json                   Output results as JSON
      --no-color               Disable ANSI colors
  -h, --help                   Print help
