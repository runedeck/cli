name: Release

on:
    push:
        tags: ["v*"]

permissions:
    contents: write

jobs:
    release:
        uses: N4M3Z/forge-cli/.github/workflows/module-release.yaml@main
        permissions:
            contents: write
