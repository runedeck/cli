name: Release

on:
    push:
        tags: ["v*"]

permissions:
    contents: write

jobs:
    release:
        uses: runedeck/rune/.github/workflows/module-release.yaml@main
        permissions:
            contents: write
