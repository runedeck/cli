# Backlog — 2026-07-20

- [ ] #rune-docs/api Second-pass refactor: rune-docs still derives clap (ListSort) and prints directly (hence the vendored sheet and its `set_no_color` global); moving to return-data + CLI rendering removes both, at the cost of restructuring the spec lifecycle bodies.
- [ ] #rune-docs/interop OpenSpec conversion deletes source directories sequentially after copying; a mid-sequence failure leaves a partial move and a retry hits destination collisions. Stage deletions like write_atomic_all stages writes.
- [ ] #rune-cli/ci CI job building and testing `--no-default-features --features assemble,validate,deploy` so the spec_root fallback resolver and the spec-less flavor cannot silently drift or rot (the dotrune_git suite behind `test-file-urls` rotted exactly this way).

- [ ] #rune-cli/install Deck-root install: `rune install --source <deck>` should iterate the deck's modules (as clean/status do) instead of failing with "no module.yaml or .rune"; today only module roots and consumers install, so home-scope deployment of a whole deck takes one invocation per module.
