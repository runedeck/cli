# Backlog 2026-07-17

From the uberSKILLS review (<https://github.com/uberskillsdev/uberSKILLS>), ranked:

- [ ] #rune-cli/validate Spec-conformance lint pass with warning severity: description lacks trigger phrasing ("use when" / "when the user"), name contains claude/anthropic (reserved), angle brackets in frontmatter fields, body under ~50 chars, agentskills.io limits (name ≤ 64 matching directory, description ≤ 1024). Errors block, warnings inform — the existing `ViolationSeverity` split carries it.
- [ ] #rune-cli/test `rune test <skill>`: headless smoke-run of a skill against a real model before deploy — stage into a temp skills dir, shell out to a configurable harness CLI (`claude -p`), report whether the skill activated.
- [ ] #rune-cli/adopt Bulk adopt from directory or zip: recursive SKILL.md discovery with symlink rejection and canonical-path containment, one adoption + provenance sidecar per skill found.
- [ ] #rune-cli/providers Provider entries for Cursor (`~/.cursor/skills`), Copilot (`~/.copilot/skills`), and Windsurf (`~/.codeium/windsurf/skills`) — paths taken from uberSKILLS source, verify against each harness's own docs before encoding.
- [ ] #rune-cli/eval Multi-model skill evaluation (OpenRouter-style) as a future `rune eval`; pairs with the deck's build-skill eval loop.

Explicitly not adopting: custom frontmatter fields (`trigger`, `model_pattern`), parse-time field whitelisting (breaks round-trip fidelity — rune must preserve unknown frontmatter byte-for-byte), display names decoupled from directory slugs, and database-as-source-of-truth deploys without manifests.
