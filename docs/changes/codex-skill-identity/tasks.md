# Codex skill identity tasks

The isolated candidate implements the identity architecture and expanded layer gate.
Final acceptance and maintainer integration remain open.
The [acceptance map](acceptance.md#implemented-test-map) identifies the implemented controls and remaining evidence
gaps.
The [validation record](validation.md) separates focused results from final checks and native evidence.

## 1. Freeze the integration contract

- [ ] 1.1 Reconcile the eight requirements and overlapping Claude layout work at the maintainer's integration
  checkpoint.
- [ ] 1.2 Accept the unnumbered ADR and allocate its canonical number through the existing sequence.
- [x] 1.3 Record the independent base revision and assign bounded implementation files without changing the active
  Claude workspace.

## 2. Establish acceptance fixtures

- [x] 2.1 Add and map deterministic controls for placement, bundles, metadata, ownership, migration, source freshness,
  and native evidence parsing.
- [ ] 2.2 Complete the remaining T01–T18 controls and required platform/native observations listed in the acceptance
  map.
- [ ] 2.3 Freeze checker and fixture revisions outside future candidate write scopes and verify external runner
  completeness.

Repository tests remain editable review artifacts. Their existence does not prove independent runner integrity.

## 3. Implement static identity checks

- [x] 3.1 Use the per-kind Codex target and preserve explicit overrides and private destinations.
- [x] 3.2 Inventory declared names, ownership, selected source identity, and observed discovery roots without choosing
  duplicate winners.
- [x] 3.3 Hash complete bundles and check local references, executable semantics, case collisions, and symlinks.
- [x] 3.4 Preserve the six resolved portable metadata fields through the existing filter.
- [x] 3.5 Add strict read-only readiness findings while preserving ordinary listing meanings.
- [x] 3.6 Bind source snapshots to authored content, selection, effective configuration, executable bytes, and
  explicit model overrides.
- [x] 3.7 Reserve `.provenance` for build sidecars and complete selected-source records without hiding authored YAML.

## 4. Add source-layer and rendered lint gates

- [x] 4.1 Add the opt-in read-only `validate --skill-layers --source` command without changing ordinary validation.
- [x] 4.2 Check portable generic and user content, provider-specific metadata and symbols, and exact configured model paths.
- [x] 4.3 Require explicit variant modes and mode-only model frontmatter without changing single-winner body resolution.
- [x] 4.4 Check every authored variant, shared text companions, and each resolved target for literal harness leakage.
- [x] 4.5 Add rendered doctor findings through the shared policy without suppressions or automatic rewrites.
- [x] 4.6 Add and execute the layer acceptance matrix with valid, invalid, malformed, unknown, and empty controls.
- [x] 4.7 Record the original deck baseline and keep its observed counts distinct from later content corrections.
- [x] 4.8 Verify complete user replacements, canonical identity, provider applicability, and metadata-only additive bodies.
- [x] 4.9 Verify resolved configuration matching, shared content selectors, unique CLI provider arguments,
  and missing-source reports.
- [x] 4.10 Verify the expanded skill/rule content fixes against a fresh deck baseline.
- [x] 4.11 Correct canonical implementation notes and repository agent guidance without changing approval statuses.

## 5. Verify the existing migration path

- [x] 5.1 Require complete ownership evidence before moving obsolete legacy bundles; refuse conflicts with
  `CSI005_MIGRATION_CONFLICT`.
- [x] 5.2 Preserve local edits, foreign destinations, untracked content, and retained legacy copies.
- [x] 5.3 Add repeat-installation, migration recovery, and competing-writer controls through the existing writer.
- [x] 5.4 Verify T14 public retry and T15 two-install coexistence through the focused 54-test deployment suite.

## 6. Verify integration and native discovery

- [ ] 6.1 Repeat local checks and independent review after the layer gate changes, then run the exact-commit
  publication gate.
- [ ] 6.2 Finish the required Linux/macOS matrix and T18 case-insensitive and native-alias evidence without silent
  skips.
- [ ] 6.3 Record N01 and N02 native catalog and companion-access evidence for the checked disposable snapshots.
- [x] 6.4 Record N03: the native catalog retains both same-name canaries, and static inspection reports
  `CSI001_DUPLICATE_NAME`.
- [x] 6.5 Resolve material findings from independent review of the expanded candidate.

Passing checks does not authorize commit, publication, or deployment.
Missing native evidence keeps its tasks incomplete even when every static check passes.
